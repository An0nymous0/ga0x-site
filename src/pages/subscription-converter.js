import React from 'react';
import Layout from '@theme/Layout';

const form = createForm()

function SubscriptionConverter() {
  return (
    <Layout title="Subscription Converter">
      <div
        style={{
          display: 'flex',
          justifyContent: 'center',
          alignItems: 'center',
          height: '50vh',
          fontSize: '20px',
        }}>

      </div>
    </Layout>
  );
}

export default SubscriptionConverter ;